fun main(){
    val text = readInput(true)
    val lines = text.lines()
//    val groups = text.split("\n\n")
//        .map { it.lines() }
//    val vals = lines.map { it.toInt() }
//    val vals = lines.map { Regex("""()""").find(it)!!.destructured }
//                    .map { (a,b) -> Pair(a.toInt(), b.toInt()) }
    val p1 = 0
    val p2 = 0
    println(p1)
    println(p2)
}